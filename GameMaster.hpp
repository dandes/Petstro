#ifndef GAMEMASTER
#define GAMEMASTER

#include <iostream>
#include <string>
#include <cstdlib> 

using namespace std;

enum MenuOption{
	MENU_SOLO_PLAY,
	MENU_TWO_PLAYERS,
	MENU_END,
	MENU_INVALID
};

enum GameplayOption{
	GAME_ROCK,
	GAME_PAPER,
	GAME_SCISSORS,
	GAME_END,
	GAME_INVALID
};



#endif
