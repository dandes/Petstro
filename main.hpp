#ifndef ROCK_MAIN_H
#define ROCK_MAIN_H

using namespace std;

enum MenuOption{
	MENU_SOLO_PLAY,
	MENU_TWO_PLAYERS,
	MENU_END,
	MENU_INVALID
};

enum GameplayOption{
	GAME_ROCK,
	GAME_PAPER,
	GAME_SCISSORS,
	GAME_END,
	GAME_INVALID
};

#endif
